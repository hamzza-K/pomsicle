# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Pydantic for data validation
pydantic>=2.0.0

# Existing dependencies (add your project dependencies here)
# These should match your main requirements.txt
polars-lts-cpu

# Additional dependencies that may be needed
python-multipart>=0.0.6  # For file uploads if needed

